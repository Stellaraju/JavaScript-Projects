// Declare two variables with string values
let Sent1 = "This is the beginning of the string";  // First part of the sentence
let Sent2 = " and this is the end of the string";   // Second part of the sentence

// Concatenate Sent1 and Sent2 to form one complete string
let fullSentence = Sent1 + Sent2; // Use the + operator to combine the strings

// Display the concatenated string using the alert method
window.alert(fullSentence); // This will show the full string in an alert box

// Write the concatenated string to the webpage using document.write()
document.write(fullSentence); // This will display the string directly on the webpage

// Add a comment explaining the purpose of each method
